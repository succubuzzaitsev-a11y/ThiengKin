# M2.1 — Visual Redesign + Google Places Enrichment

**Date:** 2026-07-14 → 2026-07-15
**Branch:** `main`
**APK:** 23.1 MB · Build SUCCESSFUL

---

## 🎯 Scope

1. **M2 Visual Redesign** — apply LINE MAN-style UI to Travel Home (full) + Near Me (minimal)
2. **M2.1 Image fix** — correct 10 category grid drawables to match `reference_thai_food_grid.png`
3. **M2.1 Category filter** — wire `CategoryGrid.onItemClick` → filter restaurants by category
4. **Google Places enrichment** — push 178 real BKK+Nonthaburi restaurants to Supabase

---

## 📝 Code changes

| File | Change |
|---|---|
| `ui/components/Pill.kt` | + `PillVariant.Solid` + `leadingIcon: ImageVector?` |
| `ui/components/SearchInput.kt` | + `showMic: Boolean = false` + `onMicClick` |
| `ui/components/TopBarAvatar.kt` | NEW (Solid gradient red + Plain gray) |
| `ui/components/CategoryGrid.kt` | NEW (5×2 LazyVerticalGrid + 10 real food photos) |
| `ui/components/AdSlot.kt` | NEW (110dp dashed + "โฆษณา" label) |
| `ui/components/CompactRow.kt` | NEW (76×76 + ETA tag + ♡) |
| `ui/components/ProvincePicker.kt` | Bug fix — remove stale closure `onDistrictSelected(null)` |
| `ui/screens/travel/TravelHomeScreen.kt` | + 10 category icons in home, wire to grid |
| `ui/screens/nearme/NearMeScreen.kt` | + 3 changes (SearchInput+AdSlot+CompactRow) |
| `ui/screens/travel/TravelHomeViewModel.kt` | + `_selectedCategoryKey`, `CATEGORY_FILTERS` (10 slots), `selectCategory()` |
| `res/drawable/category_*.{jpg,png}` | 10 food images (rice, cafe, bakery, papaya, salad, pub, etc.) |
| `.gitignore` | + `category_backup/`, `category_downloads/`, `category_images/` |

---

## 💾 Data — Supabase (post M2.1)

| Source | Rows | Notes |
|---|---|---|
| `osm` | 33,442 | nationwide (M3 sweep) |
| `google_places` | **178** | BKK 132 + Nonthaburi 46 |
| `foursquare` | 0 | not enabled |
| `manual` | 0 | not used |
| **Total** | **33,620** | |

### google_places per category

| Slot | BKK | NT | Total | Filter strategy |
|---|---|---|---|---|
| 🍜 ก๋วยเตี๋ยว | 15 | 3 | 18 | name contains ก๋วยเตี๋ยว/บะหมี่/ราเมง/เฝอ |
| 🍚 ข้าวราดแกง | 13 | 3 | 16 | name has ข้าว + (cuisine:thai OR category=ร้านอาหาร) |
| ☕ คาเฟ่ | 15 | 11 | 26 | category=คาเฟ่ OR tags has coffee_shop |
| 🍔 ฟาสต์ฟู้ด | 7 | 6 | 13 | category=ฟาสต์ฟู้ด OR tags has burger/pizza |
| 🍰 เบเกอรี่ | 16 | 3 | 19 | name contains เบเกอรี่/เค้ก/ขนมปัง |
| 🌶️ ส้มตำ | 8 | 2 | 10 | name contains ส้มตำ/ตำ |
| 🥗 สลัด | 10 | 5 | 15 | name contains สลัด |
| 🍺 ผับบาร์ | 17 | 5 | 22 | name contains ผับ/บาร์ OR amenity:pub/bar |
| 🍨 ของหวาน | 12 | 2 | 14 | tags has ice_cream OR name has ของหวาน/ไอศกรีม |
| 🕛 เปิดดึก | 19 | 6 | 25 | opening_hours has close ≥ 22:00 or < 03:00 |
| **Total** | **132** | **46** | **178** | |

---

## 🚀 Scripts (new)

| Script | Purpose |
|---|---|
| `scripts/enrich-google-places.mjs` | Push 178 google_places rows to Supabase (upsert idempotent) |
| `scripts/migration-003-google-places.sql` | Add `google_place_id` column (NOT applied — DB password rotated) |

> **Note:** `google_place_id` stored in `tags` array as `gplaceid:synthetic_N` (column migration deferred — DB password has been rotated, service_role still works for DML but not DDL through pooler)

---

## 📊 Demo HTML

| File | Purpose |
|---|---|
| `demo/home-redesign.html` | Travel Home design spec (approved) |
| `demo/nearme-focused.html` | Near Me 3-change scope (approved) |
| `demo/map-filter-mockup.html` | Map + filter mockup (60 → 178 BKK+NT restaurants with reviews) |
| `demo/cmu-restaurants.json` | CMU OSM data (200 rows) |
| `demo/bkk-restaurants.json` | BKK+Nonthaburi OSM data (1000 rows) |
| `demo/bkk-top-by-slot.json` | Per-slot top 8 OSM data |
| `demo/google-places-payload.json` | First 3 google_places payloads (sample) |
| `demo/assets/` | 10 category images source |
| `category_downloads/` | 5 generated food images + 8 GMap CDN attempts |
| `category_images/` | Original OSM source images |

---

## ⚠️ Known issues / pending

1. **Photos** — `photo_url = null` for all google_places (GMap CDN requires API key)
   - Skipped per user direction (2026-07-15)
   - Options later: image_synthesize per restaurant, or Google Places API key
2. **`google_place_id` column** — pending DDL (DB password rotated, pooler not reachable)
   - Currently stored in `tags` as `gplaceid:synthetic_N`
3. **Photos in DB** — `bakery.jpg` was a placeholder (papaya image, same SHA as papaya.jpg)
   - Replaced with real chocolate cake via `image_synthesize` (commit `6fde582`)
4. **Screencap broken on HUAWEI Mate 20 X** — can't get visual confirmation of app state
   - User must verify on device
5. **adb device not connected** — can't auto-install during this session

---

## 🔑 Memory updates

- ✅ "ThiengKin M2.1 — category image fix" (gotcha, 2026-07-15)
- ✅ "Rule: Don't build/install APK until user orders" (cross-project, 2026-07-15)

---

## 📌 Commits on `origin/main`

| SHA | Message |
|---|---|
| `6fde582` | `fix(android): M2.1 — correct all 10 category grid drawables to match reference` |
| `a09de2b` | `docs(todo): mark M2.1 image fix done (2026-07-15)` |

**Uncommitted** (working tree, NOT pushed):
- `TravelHomeViewModel.kt` — CATEGORY_FILTERS + selectCategory
- `TravelHomeScreen.kt` — wire CategoryGrid
- `scripts/enrich-google-places.mjs` — new enrichment script
- `scripts/migration-003-google-places.sql` — schema migration
- `demo/` — mockups + payloads
- `category_downloads/`, `category_backup/`, `category_images/` — gitignored

---

## 🎯 Next milestones (candidates)

| # | Milestone | Description |
|---|---|---|
| 1 | **Install + test on device** | verify category filter works on real phone |
| 2 | **Commit M2.1 logic** | push ViewModel + Screen changes to origin/main |
| 3 | **TODO + memory cleanup** | 25.7KB memory, 355 lines (over budget) |
| 4 | **Demo HTML final** | integrate 178 NT+BKK into map-filter-mockup.html |
| 5 | **M6: Auth** | Supabase email + Google Sign-In |
| 6 | **M7: Reviews + GPS check-in** | user reviews + location-based check-in |
| 7 | **More sources** | Foursquare API + Chiang Mai manual 292 places |
| 8 | **Photo enrichment** | image_synthesize 134 unique food images |
| 9 | **M3.d Android client** | refreshArea on-demand + push notifications |

---

## 📂 Project structure (key paths)

```
D:\thiengKin\
├── android\
│   ├── app\src\main\java\com\thiengkin\
│   │   ├── ui\components\          # Pill, SearchInput, TopBarAvatar, CategoryGrid, AdSlot, CompactRow
│   │   ├── ui\screens\travel\     # TravelHomeScreen + ViewModel
│   │   ├── ui\screens\nearme\     # NearMeScreen + ViewModel
│   │   ├── data\                  # Restaurant entity + DAO + Repository
│   │   └── util\                  # Haversine
│   └── app\src\main\res\drawable\  # 10 category_* images
├── scripts\
│   ├── enrich-google-places.mjs   # 178 rows to Supabase
│   ├── apply-migrations.mjs       # DB DDL runner
│   └── migration-003-google-places.sql  # schema migration (not applied)
├── data\
│   ├── osm-*.json                  # 33,442 OSM rows (M3 nationwide sweep)
│   ├── thailand-geography.json     # 77 provinces + 928 districts reference
│   └── ...
├── demo\                          # Mockups + payloads
├── category_downloads\            # Generated food images (gitignored)
├── category_backup\               # Old wrong-format files (gitignored)
├── category_images\               # OSM source images (gitignored)
├── TODO.md
└── .mavis\
    └── M2.1-SUMMARY.md            # ← this file
```

---

## 👤 User preferences locked-in

1. **"ห้าม build/install APK จนกว่าจะสั่ง"** (2026-07-15) — always show preview first
2. **"ทำ demo ก่อน ห้ามแก้ production"** — additive-only changes, never remove features
3. **"คง Concept ปัจจุบันไว้ + ปรับ UI/UX ให้เหมือน demo"** — keep existing functionality
4. **"ไม่ชอบ confirm ซ้ำๆ"** — be decisive, give recommendations
5. **"ถ้าเจอปัญหา ให้บอก root cause + แก้ให้เลย"** — diagnose and fix, don't ask
6. **"ชอบภาษาไทย"** — respond in Thai
